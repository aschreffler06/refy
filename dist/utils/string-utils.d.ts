export declare class StringUtils {
    static truncate(input: string, length: number, addEllipsis?: boolean): string;
    static escapeMarkdown(input: string): string;
    static stripMarkdown(input: string): string;
}
